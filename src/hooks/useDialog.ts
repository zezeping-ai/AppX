/** 程序化 Dialog，语义别名，实现与 useModal 一致 */
export { useModal as useDialog } from "@/hooks/useModal";
export type { ModalCreateOptions as DialogCreateOptions, ModalSlotMap as DialogSlotMap } from "@/hooks/useModal";
